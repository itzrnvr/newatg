import {Text, View} from 'react-native';

function PackagesScreen() {
    return (
        <View style={{flex: 1, justifyContent: 'center', alignItems: 'center'}}>
            <Text className={'text-slate-900'}>Packages!</Text>
        </View>
    );
}

export default PackagesScreen
